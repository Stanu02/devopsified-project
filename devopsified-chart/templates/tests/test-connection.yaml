apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devopsified-chart.fullname" . }}-test-connection"
  labels:
    {{- include "devopsified-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devopsified-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
