apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopsified-chart.fullname" . }}
  labels:
    {{- include "devopsified-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopsified-chart.selectorLabels" . | nindent 4 }}
